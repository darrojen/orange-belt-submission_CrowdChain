# Contract IDs printed by `stellar contract deploy` (see README "Deploy" section)
NEXT_PUBLIC_CROWDFUNDING_CONTRACT_ID=
NEXT_PUBLIC_REGISTRY_CONTRACT_ID=

# Soroban RPC endpoint — testnet default, override only if using a different provider
NEXT_PUBLIC_SOROBAN_RPC_URL=https://soroban-testnet.stellar.org
